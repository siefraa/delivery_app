name: delivery_app
description: Complete delivery application with admin, customer, and rider interfaces
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.6
  
  # For future integration:
  # http: ^1.1.0
  # firebase_core: ^2.24.2
  # firebase_auth: ^4.16.0
  # cloud_firestore: ^4.14.0
  # google_maps_flutter: ^2.5.0
  # geolocator: ^10.1.0
  # geocoding: ^2.1.1
  # provider: ^6.1.1
  # shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
